from selenium.webdriver.common.by import By


class MainPageLocators(object):  # defines all of the locators for the main page
	GO_BUTTON = (By.ID, "submit")

class SearchResultsPageLocators(object):
	pass
